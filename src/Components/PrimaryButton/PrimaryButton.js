import React from 'react';

const PrimaryButton = () => {
    return (
        <button className="btn text-white bg-gradient-to-r from-secondary to-primary">Get Started</button>
    );
};

export default PrimaryButton;